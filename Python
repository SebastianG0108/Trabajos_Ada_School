entero = 42
flotante = 3.141516
cadena = "Hola, mundo!"
booleano = True


resultado_concatenacion = cadena + " El entero es: " + str(entero) + ", el flotante es: " + str(flotante) + ", y el booleano es: " + str(booleano)

# Límites de enteros y flotantes en Python
#limite_enteros = "El límite de los enteros en Python está determinado por la arquitectura de la máquina, por lo que puede variar. En la mayoría de las plataformas, es 2^31 - 1 o 2^63 - 1 para enteros con signo."
#limite_flotantes = "Los flotantes en Python siguen el estándar IEEE 754. El límite superior para los flotantes de 64 bits es aproximadamente 1.8 × 10^308, y el límite inferior es de alrededor de 5.0 × 10^-324."

# Fórmula para la suma de los primeros n números pares
n = entero
suma_pares = n * (n + 1)

# Imprime los resultados
print("Resultado de la concatenación:", resultado_concatenacion)
print("Suma de los primeros", n, "números pares:", suma_pares)
